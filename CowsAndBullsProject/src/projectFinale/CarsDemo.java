package projectFinale;

public class CarsDemo {

	public static void main(String[] args) {
		// create a panel with two columns 
		// left one for game field(5 lanes for cors moving againt our car)
		// the other column will be for the score, car speed and all other info lives also
		//create a struct cars- to store info for one car
		while(true){
			// move our car (check if any key is pressed)
			//move cars
			// check if our car is hit by the other cars
			//== if we are hit and there are lives left lives--; if no lives lef game over
			// erase the playfield
			//redraw playfield(draw cars draw score ect.)
			// draw info - score, lives
			//thread.sleep()
			
		}
	}

}
